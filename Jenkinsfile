pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                script {
                    // Comando equivalente en Windows (sin nohup)
                    bat 'start /B comando_en_segundo_plano'
                }
            }
        }
    }
}

